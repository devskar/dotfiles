#!/usr/bin/env sh

url="$(git remote get-url --push origin \
  | sed -E 's#^git@([^:]+):#https://\1/#; s#\.git$##')"

case "$(uname -s)" in
  Darwin)
    open "$url"
    ;;
  Linux)
    xdg-open "$url"
    ;;
  *)
    echo "Unsupported OS: $(uname -s)"
    exit 1
    ;;
esac
